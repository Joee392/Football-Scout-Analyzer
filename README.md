# Football Scout Analyzer - Backend Setup
To run this project locally with the database:
1. Download all files and place them in your XAMPP `htdocs` folder.
2. Open phpMyAdmin and create a database named `football_db`.
3. Import the `football_db.sql` file included in this repository.
4. Access the project via `http://localhost/football_project/`.